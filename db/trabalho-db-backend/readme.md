## Para rodar o projeto:

### 1. Instalar yarn

### 2. Instalar os módulos
`yarn` ou `npm install`

### 3. Rodar o Banco de Dados
`docker-compose up -d`

### 4. Rodar a migration (criar tabelas)
`yarn db`

### 5. Rodar o projeto do backend
`yarn start`

### 6. (opcional) Inserir dados no BD
`yarn seed`